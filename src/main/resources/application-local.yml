spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:15432/mindlog}
    username: ${DB_USERNAME:mindlog}
    password: ${DB_PASSWORD:mindlog-local-only}

  jpa:
    open-in-view: false
